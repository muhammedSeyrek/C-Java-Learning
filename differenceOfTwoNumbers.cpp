#include<iostream>
#include<cmath>
using namespace std;
int main(){
	int a, b, difference = 0;
	cin >> a >> b;
	difference = abs(a - b);
	cout << "Difference two of two numbers = " << difference;
}
